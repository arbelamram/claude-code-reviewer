name: Claude Code Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

  workflow_dispatch:
    inputs:
      pr_number:
        description: 'PR number to review'
        required: true
        type: number
      owner:
        description: 'Repository owner'
        required: false
        type: string
      repo:
        description: 'Repository name'
        required: false
        type: string

jobs:
  code-review:
    if: vars.ALLOWED_REVIEWERS != '' && ((github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository && contains(fromJSON(vars.ALLOWED_REVIEWERS), github.event.pull_request.user.login)) || (github.event_name == 'workflow_dispatch' && contains(fromJSON(vars.ALLOWED_REVIEWERS), github.actor)))
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read
      issues: write
      statuses: write
    
    steps:
      - name: Block merge while review is running
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v8
        with:
          script: |
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.payload.pull_request.head.sha,
              state: 'pending',
              description: 'Code review in progress...',
              context: 'code-review/issues', // source of truth: src/constants.ts STATUS_CONTEXT
            });

      - name: Checkout code reviewer
        uses: actions/checkout@v6
        with:
          repository: arbelamram/claude-code-reviewer
          ref: main

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build Project
        run: npm run build

      - name: Run Claude Code Review
        timeout-minutes: 10
        env:
          GITHUB_OWNER: ${{ inputs.owner || github.repository_owner }}
          GITHUB_REPO: ${{ inputs.repo || github.event.repository.name }}
          PR_NUMBER: ${{ inputs.pr_number || github.event.pull_request.number }}
          CLAUDE_API_KEY: ${{ secrets.CLAUDE_API_KEY }}
          GITHUB_TOKEN: ${{ github.token }}
        run: npm run review